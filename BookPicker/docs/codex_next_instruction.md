今回は Yondoku のフロントエンド実装として、タイトル検索機能を完成させてください。

あわせて、前回実装した新規Book登録パネルの案内文を簡潔な表示へ修正してください。

最初に以下を確認してください。

docs/bookpicker_codex_current_spec.md

現在の wwwroot/index.html

現在の wwwroot/css/style.css

現在の wwwroot/js/app.js

BooksController

FilterRequest

FilterCriteria

FilterService

現在の GET /api/books のQuery Parameter

既存テスト


今回の主目的は、画面上部にすでに存在するタイトル検索欄、部分一致、完全一致のUIを実際のBooks APIへ接続し、登録されているBookをタイトルで検索できるようにすることです。

タイトル検索ロジックそのものをJavaScript側へ再実装せず、現在バックエンドに存在するタイトル検索機能を利用してください。


今回の作業対象は以下です。

タイトル検索入力欄の有効化。

検索ボタンの有効化。

Enterキーによる検索。

部分一致検索。

完全一致検索。

検索結果の一覧再描画。

表示件数の更新。

検索結果が0件の場合の表示。

検索失敗時のエラー表示。

検索状態と現在選択中Bookの整合性。

新規登録パネル上部の案内文修正。


今回は以下には進まないでください。

左側詳細フィルターの動作。

クイック条件の動作。

フィルターリセット。

お気に入り。

画像アップロード。

表紙変更。

パネル開閉アニメーション。

大規模なレスポンシブ調整。


バックエンドのController、Request、Service、Bookモデル、DbContext、Migration、SQLiteデータベースは変更しないでください。

現在存在する GET /api/books とタイトル検索のQuery Parameterを使用してください。

現在のAPI仕様では実装できない問題を発見した場合は、勝手にバックエンドを変更せず、そこで停止して報告してください。


最初に、新規登録パネルの案内文を修正してください。

現在表示されている、

これから読みたい本を登録します。すべての項目を入力してください。

という趣旨の説明は削除してください。

「本を追加」ボタンを押して新規登録パネルを開いているため、本を登録する画面であることをもう一度説明する必要はありません。


代わりに、フォームの最初の入力項目の直前へ、以下の趣旨の簡潔な補助文を表示してください。

各項目を入力・選択して、「登録」を押してください。


この補助文は大きな説明ボックスにはしないでください。

小さめのグレー系テキストとして表示し、入力フォームより視覚的な優先度を低くしてください。

新規登録機能そのもののロジックは変更しないでください。


ここからタイトル検索を実装してください。


タイトル検索欄について。

現在ヘッダーに存在するタイトル検索入力欄を使用してください。

別の検索入力欄を新規作成しないでください。

入力された文字列をタイトル検索条件として GET /api/books へ渡してください。


現在のBooks APIが要求するSearchTitleなどの正確なQuery Parameter名を、ControllerとRequestを確認した上で使用してください。

Query Parameter名や値の形式を推測しないでください。


部分一致と完全一致について。

現在画面上に存在する

部分一致

完全一致

の選択UIを使用してください。

デフォルトでは現在選択されている部分一致を維持してください。


部分一致が選択されている場合は、現在バックエンドが提供している部分一致検索を使用してください。

完全一致が選択されている場合は、現在バックエンドが提供している完全一致検索を使用してください。


部分一致と完全一致の判定ロジックをJavaScript側で行い、取得済みBook配列をfilterする方式にはしないでください。

検索条件をAPIへ渡し、バックエンドから返された結果を表示してください。


タイトル検索は現在バックエンドに存在する大文字小文字を区別しない検索仕様をそのまま利用してください。

JavaScript側で独自にtoLowerCaseなどを使用して検索結果を作らないでください。


検索実行について。

検索欄右側に現在存在する検索ボタンを押した場合に検索してください。

検索欄でEnterキーを押した場合にも同じ検索処理を実行してください。

検索ボタンとEnterキーで別々の検索ロジックを作らず、同じ検索関数を再利用してください。


今回は入力のたびに自動検索する必要はありません。

検索ボタンまたはEnterキーで明示的に検索する方式としてください。


検索文字列の前後に不要な空白がある場合は、APIへ送信する前にtrimして構いません。


検索欄が空の場合について。

空文字または空白だけの状態で検索した場合は、タイトル検索条件なしの一覧へ戻してください。

その際は GET /api/books をタイトル検索条件なしで再取得してください。

空文字を検索条件として無理にAPIへ送信しないでください。


検索結果について。

APIから取得したBook配列を、現在の本カード一覧描画処理へ渡してください。

既存のrenderBooksやcreateBookCardなどがある場合は再利用してください。

検索専用の別カード描画処理を作らないでください。


検索結果の件数を、現在画面右側に表示している

表示件数: ○冊

へ反映してください。


検索結果が0件の場合は、空白の一覧だけを表示しないでください。

例えば、

該当する本がありません。

という趣旨の簡潔なメッセージを一覧領域へ表示してください。


検索結果0件はエラーではありません。

通常の検索結果として扱ってください。


検索失敗時について。

GET /api/books が失敗した場合はConsoleへの出力だけで終わらせないでください。

本一覧領域にユーザー向けの簡潔なエラーメッセージを表示してください。

検索失敗を0件の検索結果と同じ表示にしないでください。


検索通信中について。

必要であれば検索ボタンを一時的にdisabledにし、二重送信を防いでください。

ただし今回、大きなローディング画面や複雑なスピナーを追加する必要はありません。


一致方式の切り替えについて。

部分一致と完全一致を切り替えただけでは、自動的に検索を再実行しなくて構いません。

次に検索ボタンまたはEnterキーを押したときに、現在選択されている一致方式を使用してください。


ただし、現在のUIとして一致方式を切り替えた瞬間に検索した方が自然であり、既存構造でも簡潔に実装できると判断した場合は、その方式を採用して構いません。

その場合は作業終了時に理由を報告してください。


検索状態について。

現在入力されている検索文字列と一致方式をJavaScript側の検索状態として必要な範囲で管理してください。

今後、左側詳細フィルターやクイック条件を追加したときに同じ GET /api/books のQuery Parameterへ検索条件を組み合わせられる構造を意識してください。


ただし今回、将来機能を先回りして大規模な検索状態管理システムを作らないでください。

現在必要な範囲で単純かつ拡張しやすい構造にしてください。


現在選択中Bookとの関係について。

タイトル検索後も、現在詳細表示しているBookが検索結果に含まれている場合は、可能な限り選択状態と詳細パネルを維持してください。


現在詳細表示しているBookが検索結果から外れた場合は、そのBookだけが一覧に存在しないのに詳細パネルには残り続ける不自然な状態を避けてください。

その場合は詳細パネルを閉じ、選択状態を解除する方式を推奨します。


ただし既存状態管理との整合性を確認し、より自然で簡潔な方法があれば採用して構いません。

採用した方式を作業終了時に報告してください。


検索後に本をクリックした場合は、現在実装済みの GET /api/books/{id} による詳細表示をそのまま利用してください。

詳細表示の仕組みを検索用に別実装しないでください。


新規登録、編集、進捗更新、読了、読了解除、削除との関係について。

現在正常に動作している以下の機能を壊さないでください。

新規登録。

詳細表示。

CurrentPage直接更新。

一般編集。

保存。

キャンセル。

読了。

読了解除。

削除。


特に、検索中にBookを編集した場合について考慮してください。

編集成功後の一覧再取得で、現在のタイトル検索条件を失って全Book表示へ戻らないようにしてください。


検索中にCurrentPage更新、読了操作、編集などによって既存コードが一覧を再取得する場合も、可能な範囲で現在のタイトル検索条件を維持してください。


つまり、一覧再取得処理が複数箇所にある場合は、

現在の検索条件を保持したままBooksを再取得できる構造

を検討してください。


ただし今回、左側フィルターやクイック条件はまだAPIへ送信しないでください。

タイトル検索条件だけを扱ってください。


新規登録成功後について。

現在タイトル検索中の場合、新しく登録したBookが現在の検索条件に一致するかどうかによって、一覧へ表示されるかどうかが決まるのが自然です。

登録成功後に検索条件を勝手に解除しないでください。

現在のタイトル検索条件を維持して一覧を再取得してください。


削除成功後についても同様に、現在のタイトル検索条件を維持して一覧を再取得してください。


検索Query生成について。

今後Genre、InterestLevel、ReadingStatus、クイック条件を同じ一覧APIへ追加する予定です。

そのため、API URL文字列を各機能で直接連結していく方式は避けてください。

URLSearchParamsなど、Vanilla JavaScriptで標準的かつ読みやすい方法を利用して、現在の検索条件からQuery Stringを構築する方法を検討してください。


ただし今回追加するQuery Parameterはタイトル検索と一致方式だけです。

まだ実装していないフィルター条件を先に追加しないでください。


CSSについて。

現在の検索欄、一致方式、検索ボタンの基本レイアウトを維持してください。

タイトル検索機能実装のために大きなレイアウト変更は行わないでください。


部分一致または完全一致のどちらが選択されているか、現在のUIと同様に視覚的に分かる状態を維持してください。


検索結果0件や検索エラーのメッセージについては、本一覧領域のレイアウトを大きく壊さない簡潔な表示にしてください。


新規登録パネルの補助文は、小さめのグレー系テキストへ変更してください。

大きな枠や背景付き説明ボックスは削除してください。


JavaScriptについて。

現在のBooks取得処理を確認してください。

検索機能を追加するために、Books取得処理を必要以上に複製しないでください。


例えば現在の検索状態をもとにQuery Stringを構築し、

fetchBooks

など既存のBooks取得処理がその条件を使用できる構造を検討してください。


既存の一覧再取得処理が複数箇所に存在する場合、検索条件を維持するために共通化できる部分は整理して構いません。

ただし大規模な全面書き換えは避けてください。


検索実行。

検索Query生成。

Book取得。

一覧描画。

エラー表示。

は役割が分かる構造にしてください。


React、Vue、TypeScript、外部検索ライブラリ、CSSフレームワークは導入しないでください。

HTML、CSS、Vanilla JavaScriptの現在の構成を維持してください。


今回変更してはいけないもの。

Bookモデル。

Controller。

Request。

Service。

DbContext。

Migration。

SQLiteデータベース本体。

API仕様。

既存FilterやSortのバックエンド実装。

左側詳細フィルターの動作。

クイック条件の動作。

フィルターリセット。

お気に入り。

画像アップロード。

表紙変更。

パネル開閉アニメーション。


実装後にJavaScriptの構文エラーがないことを確認してください。

dotnet build と dotnet test を実行してください。

現在存在する68件のバックエンドテストを変更せず、すべて成功することを確認してください。


タイトル検索の動作確認では、実SQLiteデータを変更する必要はありません。

現在登録済みのBookを使用してGETリクエストによる検索結果を確認して構いません。

実データの追加、更新、削除を自動的に行わないでください。


可能であれば以下の検索を確認してください。

タイトルの一部分による部分一致。

既存タイトル全体による完全一致。

存在しないタイトルによる0件。

空の検索欄から全件表示へ戻ること。

部分一致と完全一致で結果が異なること。


作業終了時に以下を報告してください。

変更したファイル。

新規登録パネルの案内文をどのように変更したか。

検索入力値の取得方法。

部分一致と完全一致の管理方法。

GET /api/booksへ送信するQuery Parameter。

Query Stringの構築方法。

検索ボタンの処理。

Enterキーの処理。

空検索時の処理。

検索結果0件時の表示。

検索失敗時の表示。

表示件数の更新方法。

検索後の選択中Bookの扱い。

検索条件を維持した一覧再取得の仕組み。

新規登録後に検索条件を維持する方法。

編集、進捗更新、読了、削除後に検索条件を維持する方法。

既存コードで共通化または整理した部分。

JavaScript構文確認の結果。

dotnet build の結果。

dotnet test の結果。

既存68件のテストがすべて維持されたか。

次の左側詳細フィルター実装へ進む前に確認すべき事項。


ここまで完了したら停止してください。

左側詳細フィルター、クイック条件、フィルターリセット、画像アップロード、お気に入り、アニメーションにはまだ進まないでください。